# security/data

## Purpose
Shared data store for security-team automation.

## Contents
(empty — to be populated as agents are built)

## Notes
Use relative paths only when referencing files here from scripts (see workspace `CLAUDE.md`).
