# Bootstrap

Read this first, every session — before doing anything else. This is the current picture, not history. It gets rewritten to stay accurate, not appended to.

Test for what belongs here: what would Claude need to know, walking in fresh, to be productive again in two minutes?

There is no need to ask any clariying questions here.  Simply read these items, follow the instructions and when you are ready, ask Brad what we are working on.  

## Where things stand

Core framework is built: CLAUDE.md (conventions), readme.md (index).  Each folder fits a specific team that Brad supports (Project Management Team | project-management folder, Program Management Team | program-management folder, Security Team | security folder, Service Delivery Team | service-delivery folder)

`program-management` (new 2026-07-16) has an architecture doc and context.md scaffolded — recurring, multi-client assessments of firewalls/M365 tenants/etc., client-facing reporting, self-contained from Service Delivery/PM for now. No `01-coordinator`/etc. role folders built yet; Coordinator (asset registry) is next per the build sequence in `program-management-agent-architecture.md`. Separately, `program-management/ait-patching/` (2026-07-21) is a one-off: flags devices in Failed/Not Installed patch status for account management to ticket, grouped by customer/location. See `ait-patching/scripts/patch_action_flags.py` docstring for the full rule set (Windows 10 EOL exclusion unless ESU, customer ignore list, workstations/laptops only). Not part of the numbered-role architecture — stand-alone script, run manually.

All three of `project-management`, `service-delivery`, and `security` now have a working `01-coordinator` report, each pulling live from that team's own source system (no manual exports anywhere anymore) and emailing results via `scripts/Send-ReportEmail.ps1`:
- `project-management/01-coordinator`: 3 reports (flags/PM/resource), pulls from Autotask, **scheduled** via Windows Task Scheduler ("PM Coordinator Reports", weekdays 7 AM).
- `service-delivery/01-coordinator`: 1 report (ticket flags), pulls from Autotask, `Invoke-CoordinatorReports.ps1` wired and tested (2026-07-21) but **not yet scheduled**.
- `security/01-coordinator`: grew beyond the original single Huntress-escalations report at some point without this doc being updated - as of 2026-08-13 it bundles 4 reports (Huntress escalations, Huntress incidents, RocketCyber threats, SentinelOne threats) in one `Invoke-CoordinatorReports.ps1` run, emailing bwinklesky@servit.net/ghuelsmann@servit.net/kterza@servit.net. **Scheduled and confirmed actually firing in production** (Brad confirmed 2026-08-13) - `data/input/ScriptManifest.csv` has had it enabled (Weekly, Friday) since 2026-08-11. The exact history of how/when RocketCyber and SentinelOne got folded into this coordinator (they were built as standalone collectors with "no email wrapper yet" per their own build notes above) isn't captured anywhere in this doc - worth asking Brad if it comes up, not worth guessing at now. Retired the prior `HuntressCustomerAudit-Escalations.ps1`, which was a straight copy-paste from an external script store (absolute `C:\PS\servit-msp\...` paths, hand-rolled email/settings logic) - now on workspace conventions instead.

Shared helper scripts, fix once/applies everywhere they're used - all renamed 2026-07-29 (Brad) to a `Functions-<Platform>-Common.ps1` convention so they group together in a folder listing, and Huntress's moved to the workspace root `scripts/` (was under `security/scripts/`) since Brad expects other folders to use Huntress later:
- `scripts/Functions-Autotask-Common.ps1` (was `Autotask-Functions-Common.ps1`) - Autotask API mechanics (project-management + service-delivery).
- `scripts/Functions-VA-Common.ps1` (was `VA-Functions-Common.ps1`) - `Import-Settings`, `Send-Results`, etc. Every platform-specific Functions-*-Common.ps1 dot-sources this as a sibling file (`Join-Path $PSScriptRoot "Functions-VA-Common.ps1"`, no `..` needed - they all live in the same root `scripts/` folder).
- `scripts/Functions-Huntress-Common.ps1` (was `security/scripts/Huntress-Functions-Common.ps1`) - Huntress API mechanics. Settings file (`HuntressSettings.txt`/`.csv`) moved alongside it to root `data/reference/`. Had two stale `..\..\` paths left over from the move (fixed 2026-07-29) - see feedback memory on fresh-process testing.
- `scripts/ReportFormatting-Common.ps1` - BOM-safe CSV/text writers (all three teams' coordinators).

`scripts/Invoke-ScheduledScripts.ps1` (the central dispatcher - one Task Scheduler trigger, reads `data/input/ScriptManifest.csv` + `ScriptRunState.json` to decide what's due) got substantially hardened 2026-07-29, all at Brad's request from prior production experience:
- **Timing guard**: rejects the whole run if triggered outside a configurable window (`-ExpectedHour`/`-ExpectedMinute`/`-MaxTimingDifferenceMinutes`, default 7:00 AM ±15 min) - guards against Task Scheduler misfiring at a weird time (Brad's hit this on multiple customer servers, especially around DST). `-SkipTimingGuard` bypasses it, independent of `-Force`. Logged as `[WARN]`, exits 0 (not a failure).
- **Deliberately did NOT add** a same-day dedup guard (would block a Daily script from re-running if it already succeeded today) - Brad's call: `-Force` already exists for legitimate re-runs, and the timing guard already covers the actual incident (wrong-time firing), so dedup would trade a rare/low-cost duplicate-run risk for real friction on cases where re-running today is wanted.
- **Relative manifest paths**: `Resolve-ScriptDefPath` resolves non-absolute `Path` values against the repo root (`Split-Path $PSScriptRoot -Parent`); absolute paths (the two legacy `C:\Scripts\...` entries, genuinely outside the repo) pass through unchanged. Manifest now uses relative paths for in-repo scripts.
- **Fixed a real splat-binding bug**: array-splat (`@array`) only binds positionally in PowerShell - a manifest `Arguments` value like `-DryRun` was silently landing on the target script's first positional parameter instead of being recognized as a switch (never caught before since `Arguments` had always been blank). Fixed via `ConvertTo-ArgumentHashtable` + hashtable splat for the PowerShell-script branch (python branch untouched - native argv doesn't have this problem).
- **Inter-script delay**: `-DelaySeconds` (default 30, Brad's call from past experience re: API/SMTP pacing) sleeps before each actually-executed script except the first; never a trailing wait after the last; skipped/disabled entries and `-DryRun` don't trigger it.
- **Log rotation + cleanup**: all four of this workspace's logging scripts (the dispatcher's own `Dispatcher_{yyyy-MM}.log`, plus the three `Invoke-CoordinatorReports.ps1` scripts' `scheduled-run-{yyyy-MM}.log`) now write one file per month. New `scripts/Remove-OldLogs.ps1` deletes any older than 12 months by parsing the `yyyy-MM` filename stamp (no line-level parsing needed); registered in the manifest as a Monthly job so it runs on the same hardened dispatcher.

**Critical fix, same day**: production's real Task Scheduler task ("Virtual Administrator Tasks - Daily", runs `C:\VA\scripts\Invoke-ScheduledScripts.ps1`) was silently failing before ever writing a single log line. Root cause: `$PSScriptRoot` is unreliable (empty) at the exact moment PowerShell evaluates **parameter default values**, specifically when a script is launched via `powershell.exe -File "fullpath"` (exactly how Task Scheduler runs things) - but NOT when dot-sourced/invoked from an already-running session, which is how this had been tested all along, so the bug went unnoticed through extensive earlier testing. `Invoke-ScheduledScripts.ps1`'s `$ManifestPath`/`$StatePath` and `Send-ReportEmail.ps1`'s `$SettingsPath` both had this exact anti-pattern (`[string]$X = (Join-Path $PSScriptRoot ...)` as a param default) - both fixed by moving the resolution into the script body (`if (-not $X) { $X = Join-Path $PSScriptRoot ... }`, the same safe pattern already used elsewhere in this workspace, e.g. `Get-CoordinatorProjectData.ps1`, `Connect-Autotask`). Confirmed fixed via genuine `-File` invocation (not just dot-sourcing) on both files, and Brad's confirmed it now works on the real production machine after copying the fix over.

**Not yet fixed, flagged only**: `program-management/ait-networking/scripts/fortigate/Invoke-FortiGateDiscovery.ps1` has this exact same anti-pattern (`$CustomersRoot`/`$RawDataRoot` params). Untouched since it's outside this thread's active scope - worth fixing if that script is ever run via `-File`/Task Scheduler directly.

FortiGate suite (`program-management/ait-networking/scripts/fortigate/`) grew a third collector (2026-08-10): `Get-FortiGateAdminAudit.ps1` - admin account hardening (super_admin without 2FA, no trusted-host restriction), interface admin-access exposure (admin protocols on WAN-role interfaces, http/telnet enabled anywhere), and global settings (idle timeout, lockout threshold, HTTPS redirect). Follows the same CustomerSettings/ConfigFile/interactive credential pattern and console+CSV output shape as the other two collectors; wired into `Invoke-FortiGateDiscovery.ps1` alongside them (`-SkipAdminAudit` to opt out).

Validated live against a real FortiGate (2026-08-10, Brad) - found and fixed two real bugs the dummy-host smoke test couldn't have caught: (1) `allowaccess` comes back from the API as one space-delimited string, not a JSON array - was silently dropping every WAN-exposure/insecure-protocol finding since `-in` checks were comparing the whole string instead of individual protocols; (2) PowerShell treats an empty array as falsy, so a `system/admin` call that succeeds but returns zero rows was being treated identically to a hard connection failure - now distinguished with its own warning instead of a fatal error.

**Known accepted gap**: `system/admin` (admin accounts, 2FA, trusted-host data) comes back empty for any credential short of a true full read-write `super_admin` profile - confirmed both via API key (custom profile granted read/write on every category still couldn't see it) and via a real user logged into the GUI as built-in Super Admin (read-only) - so it's not an API-vs-session auth thing, and not something grantable through profile permissions; FortiOS just doesn't expose "view other admins" to anything less than full super_admin. Brad's call: not worth provisioning a full read-write credential for a scheduled/unattended script over this, so it stays a manual GUI check for now, revisit if that changes. Consequence: the WAN-facing interface finding can only say "trusted-host coverage unknown" rather than confirm mitigation while this stays unresolved - the two findings are meant to be read together (open WAN admin ports may still be effectively closed down by per-account trusted-host restrictions, or may not be - can't currently tell which from the API).

**Golden testing rule going forward**: when validating anything that will run under Task Scheduler, test with the literal `powershell.exe -File "fullpath" <args>` invocation - not `cd` + relative dot-invocation, even in a fresh process. The two are not equivalent for `$PSScriptRoot`-in-param-defaults purposes, and only `-File` reproduces what Task Scheduler actually does.

Also on 2026-08-10, corrected two org-standard assumptions in `Get-FortiGateAdminAudit.ps1`'s global-settings check after Brad flagged them: ServIT's actual standard is HTTPS redirect **off** (`admin-https-redirect disable`), not on - was backwards from a generic-hardening assumption - and added a new check that the admin HTTPS port should be `8443` (`admin-sport`). Both values pulled out as named variables (`$StandardAdminPort`, `$StandardHttpsRedirectExpected`) near the top of the script body so they're easy to lift into a real baseline config later if an Analyst-role component ever gets built per `program-management-agent-architecture.md` - right now there's no Analyst layer for this project, so standards stay hardcoded in the collector script for now (deliberate call, not an oversight).

FortiGate suite grew a fourth collector (2026-08-10): `Get-FortiGateWanRedundancy.ps1` - checks whether a multi-WAN site actually has a working failover mechanism: SD-WAN enabled, or link monitors that are both enabled and wired to update routing on failure (`update-static-route`, not just logging/observing - Brad's "testing mode" concern). Cross-references functional link-monitor count against default-route count. Built from/validated against a reference Python tool Brad's used before (`qc-firewall-new.py`, same folder - parses "show full-configuration" text exports rather than REST API) which independently confirmed the base multi-WAN detection logic and the admin-https-redirect/admin-sport standards above. Wired into `Invoke-FortiGateDiscovery.ps1` (`-SkipWanRedundancy` to opt out).

Same session, Brad refined the multi-WAN signal itself, in two rounds. Round 1: raw WAN-role interface count (`IsMultiWanConfigured`, ≥2 interfaces with role=wan - the qc-firewall-new.py heuristic) is a false-positive risk on its own, since a WAN-role interface can be configured but never actually routed (spare/decommissioned link left in place). Added a routing cross-check - only interfaces actually confirmed in the routing table count toward `IsMultiWanActive`, which is what drives the failover-mechanism findings; a configured-but-unrouted WAN interface surfaces as its own softer informational finding instead of a false "no failover mechanism" claim. Round 2: Brad caught that round 1's cross-check against `cmdb/router/static` alone has its own blind spot - a DHCP-addressed WAN circuit gets its default route automatically from the DHCP lease, with **no entry in that table at all**, so a DHCP WAN would've been misread as an unrouted spare. Fixed by switching the source of truth to the live/active routing table (`monitor/router/ipv4`), which reflects static AND DHCP-learned routes uniformly, with the static table kept only as a fallback if the live call fails; also pulls `system/interface`'s `mode` field so an unrouted DHCP WAN (likely a dead lease/circuit - real problem) is distinguished from an unrouted static-mode WAN (likely an intentional cold spare - softer finding). The functional-link-monitor-vs-route-count cross-check was updated to use the live-routed WAN count as its denominator too, for the same reason. Both rounds only apply to non-SD-WAN sites (SD-WAN routes via its own zone/rule logic, so SD-WAN sites fall back to the raw interface count).

Smoke-tested via `-File` against an unreachable dummy host only. Unlike the other three collectors, several field names are NOT yet confirmed against a live FortiGate (see script's own `.NOTES`) - the SD-WAN response shape, `update-static-route`, router/static default-route detection, and (least confident of all, added last) `monitor/router/ipv4`'s `ip_mask`/`interface` fields and system/interface's `mode` field. Treat this script's findings with more skepticism than the other three until run live once.

`data/input/ScriptManifest.csv` now has 3 entries (Brad's edit): `PM-Invoke-CoordinatorReports`, `SD-Invoke-CoordinatorReports` (both Daily, relative paths, prefixed since both teams' scripts share the literal filename `Invoke-CoordinatorReports.ps1` and the dispatcher keys state/logs by manifest Name), and `Remove-OldLogs` (Monthly). The two disabled legacy external entries (Proofpoint, Quarterly-ClientReview) were removed.

`program-management/soc-rocketcyber/` built out 2026-08-10 (same session as SentinelOne, applying the lesson immediately): `Export-RocketCyberThreats.ps1` pulls open incidents (`/v3/incidents?status=open`) and isolated/quarantined devices (`/v3/agents?connectivity=isolated`, RocketCyber's term for network quarantine). No per-account looping at all, unlike the SentinelOne/Proofpoint template shape - RocketCyber's API explicitly returns all accounts at once when `accountId` is omitted (confirmed live), so `-ExcludedAccounts` filters client-side on the merged result instead. Fixed two bugs in `scripts/Functions-RocketCyber-Common.ps1` while wiring this up: `Set-RocketCyberApiContext` read `$Settings.Token` but the real settings file only has `ApiToken`; `Get-AllRocketCyberResults` was an empty stub (never implemented) - built as a page-number pagination loop (`{totalCount, currentPage, totalPages, dataCount, data}` envelope), not cursor-based like SentinelOne's. Researched via a Celerium PowerShell wrapper's docs (same source/author pattern as the SentinelOne research) then fully confirmed live against the real account: pagination shape, real field names for both endpoints (including the accountId/accountName vs. customerId/customerName naming inconsistency between the two endpoints - normalized to AccountName/AccountId in this script's output), and that the API hard-400s on both an unrecognized param and an invalid `status` enum value (confirmed real filtering, not silent no-ops). Live run: 0 open incidents, 11 isolated devices (Coleto Brands x6, Grey Eagle x2, GWC x1, ServIT Kennesaw x1) - genuinely current state, not test data. Old unfinished skeleton (`RocketCyberDeviceAudit-BillableDevices.ps1`, named for a different purpose - device counts for billing, matching the Proofpoint/PowerDMARC/Vipre Billables pattern, not threats/quarantine) deleted per Brad's call rather than kept as a placeholder - if a RocketCyber billables report is wanted later it'll need to be built fresh. No email wrapper yet, same open item as SentinelOne.

`program-management/soc-sentinelone/` converted from an old ad-hoc script (2026-08-10) to workspace conventions, matching `email-proofpoint/Export-ProofpointAdmins.ps1` as the template: `Export-SentinelOneThreats.ps1` replaces the untracked `SentinelOneDeviceAudit-Threats.ps1` (deleted - same "retire, don't keep alongside" call as prior conversions, and this one also had a live SentinelOne API bearer token hardcoded in plaintext, so deleting it removes that exposure too; token was never committed to git - script and `data/reference/SentinelOneSettings.txt`/`.csv` were all untracked, `.csv` properly gitignored - so no history cleanup was needed, just the conversion itself). New script pulls the token via `Import-Settings` like every other platform. Found and fixed a pre-existing blocking bug in `scripts/Functions-SentinelOne-Common.ps1` while wiring this up - `Set-S1ApiContext` had `@{ $Headers }` instead of `@{ Headers = $Headers }`, a hard syntax error that failed to parse the *entire file*, so nothing in it (including the already-fine `Get-AllS1Results`) could have loaded before this fix; also retyped its `$Settings` param from `[array]` to `[PSCustomObject]` (was only "working" by accident via PowerShell's array member-enumeration). Per Brad's call this session, built as a pure collector only - no `Invoke-SentinelOneReports.ps1`/email wrapper yet (the original script emailed bwinklesky@servit.net/tmarsili@servit.net directly; that notification is not currently replicated anywhere - worth remembering if threats stop getting surfaced to anyone).

Same session, Brad asked about the ~4.5-minute runtime (528 sites, one-agents-call-per-site). Researched SentinelOne's v2.1 API (third-party-documented, then verified live) and found `siteIds` accepts a comma-list (multiple/all sites in one call) plus server-side filters `activeThreats__gt` and `networkQuarantineEnabled` - confirmed genuine (not silently-ignored no-ops) by checking a deliberately-fake param hard-403s while these don't. Also empirically confirmed `infected=true` returns an identical result set to `activeThreats__gt=0` in a live sample, matching the API's own description of "infected" as "at least one active threat" - so the rewritten script only queries `activeThreats__gt=0`, not both. Rewrote `Export-SentinelOneThreats.ps1` around chunked (50 sites/batch, to stay under URL-length limits) server-side-filtered queries instead of per-site full-agent-pulls + client-side filtering. Validated field-by-field against a real reference run Brad had saved (`output/SentinelOneDeviceAudit-Threats.csv`, from the old script): exact match, 4/4 threats, every field identical, runtime dropped to 9 seconds for all 528 sites.

`program-management/ait-m365/` got its first Collector/Analyst pipeline 2026-08-13, peeling apart the old `katz/GroupAudit-TempGroups.ps1` monolith (actually broken - dot-sourced only `Functions-VA-Common.ps1` but called functions not defined there, and had no `param()` block so its params were never bound). Brad's scope call: 2 real stages (Collector → Analyst), not 3 - Risk/Compliance in the architecture doc is specifically *external/regulatory*, and a temp-group naming check has no regulatory dimension. Same day, a second pipeline (admin-group audit) was built the same way from another broken legacy script. Both live in `ait-m365/` itself (offering-shared, not nested under a customer) since Collector/Analyst logic is reusable across whichever customers use the audit:
- `01-collector/Collect-EntraGroups.ps1` / `Collect-EntraAdminGroupMembers.ps1` - pull raw Graph data (all groups; or all role assignments + users + license catalog, so the overlap check has the full picture), no interpretation, write to `data/raw/<customer>/`.
- `02-analyst/Compare-TempGroupsAudit.ps1` / `Compare-AdminGroupAudit.ps1` - read the raw snapshot(s) + reference data, apply the baseline, write findings to `output/<customer>/`. Never email - that stays out of the Analyst layer entirely (matches the service-delivery coordinator's Get-\*Data → Export-\*Report → Invoke-\* split). Admin-group's baseline (`Billing Administrator`/`User Administrator`/`Exchange Administrator`/`Global Administrator`/`Global Reader`, 90-day stale threshold) came from Ameriserve's old production wrapper, confirmed by Brad to be a shared template value, not Ameriserve-specific - treated as the default going forward.
- Standing pattern set for all future Analyst scripts (Brad's review pass, 2026-08-13): build output rows via `[ordered]@{...}` → cast to `[PSCustomObject]`, explicit not implicit; a column that's structurally always-blank outside a specific switch (e.g. `IsExcluded`) gets omitted from the row entirely rather than always-present-but-usually-null.
- `Map-Customer` converted from a hardcoded hashtable to `data/reference/CustomerMap.csv` (root level, since the function is workspace-shared) same day - Brad's call after the hashtable pattern caused a real bug (below). All 24 existing customers carried over, diffed by hand.

**2026-08-14 session** - pushed the wrapper split one level further and found/fixed several real bugs along the way:
- **Wrapper layer restructured**: the Collector→Analyst→email orchestration logic moved out of `katz/` and up into `ait-m365/scripts/Invoke-TempGroupsAudit.ps1` / `Invoke-AdminGroupAudit.ps1` - generic, reusable across any customer using that audit. `katz/Invoke-TempGroupsAudit.ps1` / `Invoke-AdminGroupAudit.ps1` are now genuinely thin: resolve "Katz" via `Map-Customer`, then call through to the shared script above. This is the shape new customers get onboarded into going forward - a `CustomerMap.csv` row plus one small per-audit wrapper file per customer.
- **`Map-Customer` moved again**, from `Functions-M365-Common.ps1` to `Functions-VA-Common.ps1` (Brad's call - it's workspace-shared, not M365-specific, and both katz wrappers already dot-source `Functions-VA-Common.ps1`, so this also fixed a missing-import bug the restructure had introduced). Its return object was also out of sync with the CSV, which had grown a `FromAddress` column and renamed `CustomerFolder`→`SharepointFolder` on 2026-08-13 without the function being updated - `Map-Customer` was still returning the old `CustomerFolder` name (silently `$null`, no `FromAddress` at all). Fixed to return `Directory`/`SharepointFolder`/`FromAddress` matching the real CSV columns.
- **Two smaller bugs fixed**: `ait-m365/scripts/Invoke-AdminGroupAudit.ps1` referenced an undefined `$FromEmail` instead of the actual param `$FromAddress` in both its email calls (success and failure paths) - silently sent with a blank From. Both `katz/Invoke-*.ps1` wrappers' outer catch did `Write-Host "$_.Exception.Message"` (missing `$(...)`) - printed the object plus the literal text `.Exception.Message` instead of the real error text.
- **Workspace-wide bug fixed**: `Send-Results` (`Functions-VA-Common.ps1`) caught `Send-MailMessage` failures, wrote them to console, and returned normally - never re-threw. Every caller's own try/catch (including the M365 wrappers' failure-notification logic, and every team's coordinator report that sends email) saw a failed send as a success. Added a `throw` at the end of the catch. Confirmed live (see below) this actually changes wrapper behavior from false "Run completed successfully" to correctly-logged "Run failed." Only other direct caller in the repo is `email-vipre/Export-BillableUsers.ps1`, where the call is the script's last statement - behavior there is a strict improvement (a real failure now surfaces instead of being silently swallowed), not a regression, but not independently live-tested.
- **Diverzify removed entirely** - Brad's call, it was a one-off test. Directory, the `.claude/commands/rundiverzifyuserreport.md` slash command, and its `report-routing-matrix.md` rows all deleted/cleaned up rather than left dangling.
- **Live-tested both Katz pipelines end-to-end for the first time**, via `-File` against real Graph data: temp-groups (38 groups pulled, 2 flagged, 0 excluded) and admin-group (10 licenses / 25 role assignments / 162 users pulled, 9 admin-role memberships across 5 groups flagged) both collected and analyzed correctly. Email send itself still couldn't be verified - this dev sandbox reaches Graph over HTTPS but not outbound SMTP (`Unable to connect to the remote server`) - but that live failure is exactly what proved the `Send-Results` fix works, by watching the log correctly flip from false-success to real-failure on the same run.

**2026-08-17 session** - `ait-m365` grew from 2 audits to 8 since the 2026-08-14 entry above (CA group member audit, license detail, license summary, mailbox size, Conditional Access policy audit, and named-locations audit all built out to the same Collector→Analyst→Default-orchestrator→customer-wrapper shape, outside this session's direct involvement - the new scripts' docstrings read as carefully thought through, e.g. `Collect-EntraLicenses.ps1` explicitly documents *why* it deliberately duplicates part of `Collect-EntraAdminGroupMembers.ps1`'s pull rather than reusing it, so that overlap is a documented choice, not drift). Same day, Brad renamed every wrapper-layer file across all 8 audits for consistency (e.g. `Invoke-TempGroupsAudit.ps1` → `Invoke-GroupAudit-Temp.ps1`; the shared orchestrator layer gained an explicit `-Default` suffix) and asked for the resulting stale cross-file references to be cleaned up:
- Fixed all 8 `katz/Invoke-*.ps1` → `ait-m365/scripts/Invoke-*-Default.ps1` path references - every one still pointed at its pre-rename filename, so every audit was one `& (Join-Path ...)` away from "term not recognized" the moment it actually ran.
- Fixed stale `.EXAMPLE` filenames in all 16 wrapper+orchestrator files, plus ~12 stale filename citations inside `.DESCRIPTION` provenance comments across `01-collector`/`02-analyst`.
- Fixed all 7 collectors' `-SettingsPath` fallback default, still pointing at the pre-restructure `<customer>/CustomerSettings.txt` location instead of the current `data/reference/<customer>/M365Settings.txt` - dead code today (every orchestrator always passes `-SettingsPath` explicitly) but would've silently pointed at a nonexistent file if any collector were ever run standalone.
- Verified all 25 cross-file path hops (8 wrapper→orchestrator, 17 orchestrator→collector/analyst) resolve to a real file on disk, and all 32 `.ps1` files under `ait-m365` (plus `Functions-VA-Common.ps1`) parse clean.
- Flagged naming inconsistency (`Invoke-GroupsAudit-Temp-Default.ps1`'s plural "Groups") - Brad confirmed, renamed to `Invoke-GroupAudit-Temp-Default.ps1` to match its sibling files' singular "GroupAudit" pattern; `katz/Invoke-GroupAudit-Temp.ps1`'s reference updated to match, re-verified.
- Not reviewed this session: the actual audit *logic* inside 5 of the 6 newer Collector/Analyst scripts (only cross-file references and path resolution were checked, not business rules/Graph query correctness). **CA group member audit now an exception** - Brad asked specifically about `GroupFound` Y/N parity 2026-08-17, which prompted a full field-by-field diff against the original retired script (recovered from git history, commit `8b58111`) rather than just answering the one question: group names, not-found handling, disabled-user filtering, expected-match logging, and the Compare-Object discrepancy logic all check out matching. One real (pre-existing, not port-introduced) gap found: `data/reference/katz/expected-ca-exclusions-*.csv` are all empty (header-only) - the *original* wrapper never actually passed real expected-membership values either (same unbound-parameter pattern as temp-groups' exclusions), so every current CA group member currently shows as a "not in expected list" finding. Brad's call: leave empty for now, hasn't run this one for real yet.
- Brad's going through each Collector's actual output (raw CSVs on disk) one at a time and renaming for clarity, `-<AuditSuffix>` pattern on both script and output file(s) - same fix-everywhere treatment applied to each: script rename, `Export-Utf8NoBomCsv -Path`/default-path-parameter updates inside the collector, the orchestrator's collector-path reference, the Analyst's raw-file-path default(s), any docstring citations in sibling files, delete the stale old-named raw CSV from `data/raw/katz/` (regenerates under the new name next run), then re-verify parse-clean + a synthetic/no-file run proving the *default* path resolution (not just explicit `-Path` args) actually computes the new filename. Done so far (2026-08-17):
  - `Collect-EntraAdminGroupMembers.ps1` → `Collect-EntraGroupMembers-Admins.ps1`; outputs → `EntraLicenses-Admins.csv`/`EntraRoles-Admins.csv`/`EntraUsers-Admins.csv`.
  - `Collect-EntraCaGroupMembers.ps1` → `Collect-EntraGroupMembers-CA.ps1`; output → `EntraGroupMembers-CA.csv`.
  - `Collect-EntraConditionalAccessPolicies.ps1` → `Collect-EntraPolicies-CA.ps1`; its four outputs (users/groups/roles pulled only to resolve the GUIDs policies reference, plus the policies themselves) → `EntraUsers-CA.csv`/`EntraGroups-CA.csv`/`EntraRoles-CA.csv`/`EntraPolicies-CA.csv`. First correction mid-request this round - Brad's initial output name for this one was a copy-paste leftover from the *previous* rename (`EntraGroupMembers-CA.csv`, which would've collided with the CA-group-members collector's own output); caught before any edits were made and corrected to the 4-file list above.
  - `Collect-EntraLicenses.ps1` (script name unchanged this time - only its outputs) → `EntraLicenses-Usage.csv`/`EntraLicenses-Assignment.csv` (was `EntraLicenseCatalog.csv`/`EntraLicenseAssignments.csv`). This one has 3 consumers, not 1 - `Compare-LicenseDetailAudit.ps1` and `Compare-MailboxSizeAudit.ps1` read both files, `Compare-LicenseSummaryAudit.ps1` reads only the Usage one - all three updated and verified.
  - `Collect-EntraNamedLocations.ps1` → `Collect-EntraLocations.ps1` (output file `EntraNamedLocations.csv` deliberately left unchanged - Brad's call, script name only this time). Orchestrator + Analyst docstring citation updated, no raw-path/data cleanup needed since the output name didn't move.
  - Remaining un-renamed collectors as of this entry: `Collect-EntraMailboxUsage.ps1`, `Collect-EntraGroups.ps1` - expect Brad to keep working through these; apply the same pattern when he does.
  - Same rename pass now moving through `02-analyst/Compare-*.ps1` too: `Compare-AdminGroupAudit.ps1` → `Compare-GroupMembers-Admins.ps1`, output `admingroup-audit.csv` → `GroupAudit-Admins.csv` - orchestrator (script path + `$AuditCsv` path) and 2 sibling docstring citations (`Collect-EntraGroupMembers-Admins.ps1`, `Compare-MailboxSizeAudit.ps1`) updated, stale old-named output deleted from `02-analyst/output/katz/`, and this one re-verified with an actual functional run against real Katz raw data (not just a synthetic/no-file default-path check) - 9 admin role memberships across 5 groups, matching prior runs.
  - `Compare-CaGroupAudit.ps1` → `Compare-GroupMembers-CA.ps1`, output `cagroup-audit.csv` → `GroupMemberAudit-CA.csv` - orchestrator + `Collect-EntraGroupMembers-CA.ps1` docstring citation updated. **Gotcha hit while verifying this one**: its raw input file (`data/raw/katz/EntraGroupMembers-CA.csv`) had been deleted during the *Collector's* own rename earlier and never regenerated, so the first verification run silently degraded to "every group not found" instead of erroring - this script doesn't hard-fail on a missing raw file, it just treats it as zero rows. Re-ran the Collector to regenerate it, then re-ran the Analyst for a genuine check: 3 of 4 CA groups correct, "MFA Exclusion Group not found" is a real finding (that group genuinely doesn't exist in Katz's tenant), not a rename artifact. **Worth remembering for the rest of this rename pass**: after renaming a Collector's output file, its raw data needs a fresh Collector run before the paired Analyst can be meaningfully verified - a stale/missing raw file won't error loudly, it'll just look like "everything's broken" in the findings.
  - Remaining 6 `02-analyst/Compare-*.ps1` scripts renamed in one batch 2026-08-17: `Compare-ConditionalAccessPolicyAudit.ps1`→`Compare-Policies-CA.ps1` (out: `PolicyAudit-CA.csv`), `Compare-LicenseDetailAudit.ps1`→`Compare-LicenseDetail.ps1` (out: `LicenseAudit-Detail.csv`), `Compare-LicenseSummaryAudit.ps1`→`Compare-LicenseSummary.ps1` (out: `LicenseAudit-Summary.csv`), `Compare-MailboxSizeAudit.ps1`→`Compare-MailboxSize.ps1` (out: `MailboxAudit-Size.csv`), `Compare-NamedLocationsAudit.ps1`→`Compare-EntraLocations.ps1` (out: `Locations-Audit.csv`), `Compare-TempGroupsAudit.ps1`→`Compare-Groups-Temp.ps1` (out: `GroupAudit-Temp.csv`). All 6 orchestrators + every cross-file docstring citation (including 3 scripts that cite `Compare-TempGroupsAudit.ps1`'s `-IncludeExclusions`/`IsExcluded` convention by name) updated; stale old-named outputs cleared from `02-analyst/output/katz/`; all 31 `.ps1` files under `ait-m365` parse clean. **All 6 verified with real functional runs against actual Katz data**, not just default-path checks - regenerated raw snapshots first for CA-policies and named-locations (neither had been collected yet, same gotcha as the CA-group-members rename). This closes out the full Collector+Analyst rename pass across all 8 `ait-m365` audits - naming is now consistent workspace-wide.
  - **Brad's own flagged follow-up, once the renaming pass is done**: look at consolidating the redundant users/groups exports - at least three collectors now independently pull overlapping Entra users/groups data (`Collect-EntraGroupMembers-Admins.ps1`'s users, `Collect-EntraPolicies-CA.ps1`'s users/groups/roles, and whatever `Collect-EntraGroups.ps1`/others pull) - worth assessing whether one shared users/groups snapshot could serve all of them instead of N independent pulls. Note this would be a reversal of the deliberate "every collector pulls its own data" precedent documented in `Collect-EntraLicenses.ps1` (2026-08-13) - that precedent optimized for collector independence over pull efficiency; revisit that tradeoff explicitly rather than assuming the old reasoning still wins once Brad's ready to look at it.

**New session, picking up after an overnight gap on a different machine** - opened by re-reading this file plus verifying against actual `git log`/`git status`, since bootstrap hadn't been rewritten to capture what happened after the last entry above. Found: commit `f7bf46b` (22:08 the same night) added 3 more audits (mailbox purpose/licensed-shared, licensed-disabled users, temp users) via `Collect-EntraUsers.ps1`/`Collect-EntraMailboxPurpose.ps1` + `Compare-Users-Temp.ps1`/`Compare-Users-LicensedDisabled.ps1`/`Compare-Mailboxes-LicensedShared.ps1`, and renamed `Compare-MailboxSize.ps1`→`Compare-Mailboxes-Size.ps1` cleanly (Brad fixed every reference himself, verified). Plus 4 more legacy scripts dropped in uncommitted (same three-file pattern as every prior split: thin `katz/Invoke-X.ps1` already correct, `scripts/X-Default.ps1` monolith, `scripts/X-Wrapper.ps1` = real Ameriserve production values) - `ait-m365` is now at **12 audits total**.

Built out all 4 newly-dropped-in audits to the established Collector→Analyst→Orchestrator→wrapper shape:
- **Teams (2 audits, 1 shared collector)**: `01-collector/Collect-EntraTeamsActivity.ps1` pulls the Teams activity report only (`-ReportingDays`, default 180 matching both originals' real value) - deliberately does NOT re-pull group metadata, joins against the *already-existing* `Collect-EntraGroups.ps1` raw snapshot instead (same fields needed, genuinely shared - like `Collect-EntraUsers.ps1`'s precedent, not `Collect-EntraLicenses.ps1`'s independent-pull one). `02-analyst/Compare-Teams-Activity.ps1` (reporting-only, `excluded-teams.csv`) and `02-analyst/Compare-Teams-Disable.ps1` (flags teams inactive >N days, skips newly-created ones, same exclusion file) both read it.
- **Users (2 audits, reusing last night's collectors)**: `02-analyst/Compare-Users-Disable.ps1` (flags enabled users with no logon >N days, excludes resource/shared mailboxes automatically, `-IncludeUnlicensedUsers`/`-IncludeGuests` default `$true` matching the real Ameriserve wrapper) and `02-analyst/Compare-Users-Hr.ps1` (plain non-guest/non-resource-mailbox roster) both read `Collect-EntraUsers.ps1`'s and `Collect-EntraMailboxPurpose.ps1`'s existing raw snapshots directly - no new collector needed for either, avoiding two more redundant users pulls.
- **The `-Testing` switch question**: both `-Disable` originals had a `-Testing` switch implying a real disable action existed, but it was never actually implemented - the "real" branch just wrote a status string and (in the Teams one) a `Write-Host` referencing a property, `$Result.SiteName`, that didn't even exist on the object (should've been `TeamName`); the Users one's original comment literally says `# For execution, take action on aged users (For Future Development)`. Brad's call, after briefly saying `$false` then immediately correcting to **`$true`**: keep the switch (`[switch]$Testing = $true`, PowerShell allows a non-`$false` default on a switch param) for forward compatibility since Graph access is read-only workspace-wide right now anyway, but don't build a real disable action - findings are always just flagged candidates, both branches, never a claim that something was actually disabled.
- Real bug caught during verification: `Compare-Users-Disable.ps1` called `Get-LatestDate` but only dot-sourced `Functions-VA-Common.ps1`/`Functions-Formatting-Common.ps1` - that function actually lives in `Functions-M365-Common.ps1`. Fixed (added the missing dot-source) and confirmed working on the re-run.
- All 9 new files parse clean, and all 4 pipelines run successfully end-to-end against real Katz data: Teams-Activity (19 teams), Teams-Disable (17 of 19 flagged stale), Users-Disable (18 of 72 evaluated flagged, 9 resource mailboxes correctly excluded), Users-Hr (48 of 136 non-guest users after excluding 88 resource mailboxes). Email delivery itself still unverified (same no-outbound-SMTP sandbox limitation as every other audit).
- 3 new empty (header-only) reference files created: `excluded-teams.csv`, `excluded-inactive-users.csv`, `excluded-hr-users.csv` - same "leave empty until Brad wants to populate" call as the CA-group audit's reference files.
- All 8 now-superseded legacy drop-in files deleted (`SiteAuditTeams-Activity-Default/Wrapper.ps1`, `SiteAuditTeams-Disable-Default/Wrapper.ps1`, `UserAccessAudit-Disable-Default/Wrapper.ps1`, `UserAudit-Hr-Default/Wrapper.ps1`) - same "retire, don't keep alongside" call as every prior conversion.

**Performance fix, same day** - Brad measured `Compare-Mailboxes-LicensedShared`/`Compare-Users-Disable` against a larger real customer and found the new split-pipeline versions took ~2x longer than the old monoliths (2m44s→4m57s, 1m44s→4m30s). Root cause: the old scripts filtered candidates down *before* the expensive per-user mailbox-purpose lookup; `Collect-EntraMailboxPurpose.ps1` can't do that narrowing itself (a Collector doesn't interpret anything), so it always checked purpose for every user in the tenant, with the two Analysts only narrowing after that work was already done. Brad's call on the structural question ("is this worth fixing, or a worthy tradeoff for consistency"): fix it, but without duplicating filter logic between an orchestrator pre-filter and the Analyst's real filter (a real drift risk otherwise) - **provide the collector a list of UPNs actually in scope and have it check only those**, with output going to a dedicated file so it never collides with the full/shared one Hr/Temp/LicensedDisabled still need:
- Both `Compare-Users-Disable.ps1` and `Compare-Mailboxes-LicensedShared.ps1` gained a `-ListCandidateUpns` mode - computes the real (or, for LicensedShared, a safe superset) candidate list from already-collected CSVs alone, no Graph/mailbox-purpose data needed, and exits. This keeps the candidate logic in exactly one place (the Analyst itself), called twice by the orchestrator rather than reimplemented once more elsewhere.
- `Collect-EntraMailboxPurpose.ps1` gained a `-Upns` param to scope its check instead of pulling the whole tenant. Each affected orchestrator now runs: collect users → Analyst `-ListCandidateUpns` → collect mailbox purpose scoped to that list (written to a dedicated `EntraMailboxPurpose-AccessDisable.csv`/`EntraMailboxPurpose-LicensedShared.csv`, which is now each Analyst's own default `-MailboxPurposePath` too) → Analyst normally.
- **Real bug caught during verification, not by inspection**: first implementation skipped the "list all users" id lookup entirely and batched the purpose check directly by UPN (Graph's `/users/{id}` endpoints accept a UPN in that position, so this looked like a legitimate extra optimization). Diffing the scoped result against a full-tenant baseline turned up 3 silently-missing rows. Root cause confirmed live, side-by-side, same mailbox: querying `mailboxSettings` by object id succeeds every time; querying the *identical* endpoint by UPN for a "shared"-purpose mailbox returns a **persistent** 503 `MailboxInfoStale` - not transient, still failing after 4 retries over 46 seconds. Since these two audits specifically target resource/shared mailboxes, that failure mode hits exactly the accounts they most need to see. Fixed by always resolving object ids first (the "list all users" call was never the expensive part) and batching by id, filtered down to the requested UPNs - the `-Upns` optimization is real and unchanged, only the "skip id resolution too" half of the original attempt was wrong.
- Also added while investigating (a real, independent gap, not a symptom of the above): the collector now retries individual 429/503/504 batch sub-responses up to 4 times honoring `Retry-After`, and warns instead of silently dropping a row if still failing after all retries - previously any transient per-user Graph error just silently vanished from the raw snapshot with zero indication anything was missed, in both scoped and unscoped mode.
- Both audits re-verified byte-for-byte identical to a full-tenant baseline (18/18 rows and 1/1 row) after the id-based fix, on top of the earlier structural fix that already caught the `Get-LatestDate` missing-dot-source bug.
- Applied the same "Analyst owns its own default" consistency fix from the switch-plumbing discussion above to nothing new here, but the -ListCandidateUpns pattern itself is now the template for any future Collector that's expensive enough to need scoping - reuse it rather than re-deriving another wrapper-level pre-filter.

**Fifth batch, same day** - 5 more legacy scripts dropped into `ait-m365/scripts/` for conversion (same 3-file pattern: thin `katz/Invoke-X.ps1` - this time *not* pre-built, unlike the last batch, so built those too - `scripts/X-Default.ps1` monolith, `scripts/X-Wrapper.ps1` real Ameriserve values), bringing `ait-m365` to **17 audits**:
- **Two don't fit the Collector→Analyst shape at all**: `LogAudit-UserActivities`/`LogAudit-UserSignIns` are raw log exports (Entra directory-audit and sign-in logs over a lookback window, zero filtering/flagging in the originals) - built as Collector-only, matching the SentinelOne precedent, no Analyst. Brad's noted future direction once these are worth a second look: an Analyst that flags sign-ins that would've been blocked if not for a Conditional Access policy, or a failed-vs-successful rollup to gauge how hard an org's being targeted - not built yet, deliberately just the raw pull for now.
- Both log collectors preserve the original's size-aware email behavior on Brad's explicit ask: the raw file is always written locally regardless of size; the orchestrator only attaches it if under 30MB, otherwise the email just says where to find it on the host instead of attaching - "this way we could go and scoop it up." This logic lives in the orchestrator (`Invoke-LogAudit-*-Default.ps1`), not the Collector, since it's an email decision.
- `SiteAuditOneDrive-Activity`, `SiteAuditSharepoint-Activity`, `SiteAuditSharepoint-Disable` all fit the established shape cleanly - Activity/Disable share one `Collect-EntraSharepointActivity.ps1` (same pattern as Teams). That collector also resolves `IsTeamBacked` (does a SharePoint site's name match a Team-backed group) as a reference-data join, so `Compare-Sharepoint-Disable.ps1` never double-flags a site that `Compare-Teams-Disable.ps1` already covers.
- **Two real bugs found and fixed during this batch**, both in shared code, not the new scripts:
  1. `Functions-M365-Common.ps1`'s `Write-IncrementalCsv` called `Ensure-Directory`, which doesn't exist anywhere in this workspace (would have thrown "term not recognized" the first time anything actually used it against a new path - it's the first real consumer of this function since the framework was built). Brad's call after a brief detour (first asked for a real `Ensure-Directory` function to be added, then reversed that): fixed to call `Test-Directory` (`Functions-VA-Common.ps1`) instead, passing its parent directory since `Test-Directory` expects a directory path, not a file path. Verified with nested-directory creation and the exact calling convention every legacy script uses (`Write-IncrementalCsv -Path X -InputObject Y`, not piped - confirmed the pipeline-style calling convention has its own unrelated `begin{}`-block quirk that nothing in this codebase actually exercises, so it wasn't worth fixing).
  2. `Collect-EntraSharepointActivity.ps1`'s `IsTeamBacked` resolution called `.ContainsKey($SiteInfo.name)` without checking for a null name first - threw "Key cannot be null" live during verification (at least one real site in Katz's tenant has no resolvable name). Fixed with a null guard; re-verified row count went from 41 to the correct 42 after the fix (the previously-erroring site's row was being dropped, not just missing one field).
- All 5 new audits + the 2 already-existing pipelines re-verified against real Katz data after both fixes: OneDrive (40 accounts), SharePoint Activity (41 in-scope sites) + Disable (14 of 20 non-Team-backed sites flagged, 21 correctly excluded as Team-backed), and both log collectors (spot-verified with a 1-day lookback rather than the full 30-day default, including confirming `-ReportOnly` genuinely narrows the row count). All 72 `.ps1` files under `ait-m365` parse clean.

## Open loops

- Of the 17 `ait-m365` audits, only temp-groups, admin-group, and CA-group-members have had a real logic diff-review against their original scripts (not just "does it run"). The rest (license detail/summary, mailbox size, CA policy, named locations, the 3 built 2026-08-17 night on the other machine, the 4 Teams/Users ones, and the 5 in this latest batch - OneDrive/SharePoint Activity, SharePoint Disable, and the 2 log-only exports) have only been confirmed to run successfully against real Katz data, not deep-reviewed.
- The 2 log-export audits (`LogAudit-UserActivities`/`LogAudit-UserSignIns`) have no Analyst layer yet by design - Brad's flagged wanting one eventually (CA-bypass/"bad actor" detection, or a failed-vs-successful sign-in rollup) but it's not built. Worth remembering as a real future ask, not a gap to silently fill in.
- Actual email delivery is still unverified for all 12 audits - this dev sandbox has no outbound SMTP. Needs a real send test from an environment with SMTP access before any of them go near a scheduled task. None are in `ScriptManifest.csv`/the central dispatcher yet - still manual/standalone runs.
- `Invoke-FortiGateDiscovery.ps1`'s same `$PSScriptRoot`-param-default bug (see above) - not fixed, just flagged.
- Working tree has had uncommitted changes carried across multiple sessions now (nothing alarming - own edits plus everything built across the last few sessions). Worth asking Brad whether/when he wants a commit checkpoint.

**Workspace-wide bug found and fixed 2026-08-17**: `scripts/Functions-Formatting-Common.ps1`'s `Export-Utf8NoBomCsv` (`-InputObject [Parameter(Mandatory)][array]`) crashed on a genuinely empty result set - "Cannot bind argument to parameter 'InputObject' because it is an empty collection" - a PowerShell binder quirk where Mandatory + explicitly-typed array/string parameters reject empty values outright, before the function body runs. Found live by Brad running `Invoke-MailboxAudit-Size.ps1` against real Katz data: 121 mailboxes audited, 0 over the 75% threshold - a legitimate clean result, not an error, but the script crashed trying to write it. Fixed with `[AllowEmptyCollection()]`/`[AllowEmptyString()]` (the fix itself hit the same quirk one layer down, in `Set-Utf8NoBomContent`'s `$Value` param, before landing); empty input now writes a genuine 0-byte file instead of crashing. This function is used by 21 files across all four teams (ait-m365, security, project-management, service-delivery), so any of them could have hit the same crash the first time their result set was legitimately empty - worth keeping in mind if a coordinator report ever "randomly" fails on an otherwise-clean day. Verified: parses clean, empty-array case no longer throws, non-empty case byte-for-byte unchanged (still no BOM), and `Compare-MailboxSizeAudit.ps1` re-run against Brad's actual real raw data now succeeds with the same 0-result outcome.

## Recent decisions worth knowing

- program-management scope: multi-client/MSP-style, client-facing deliverables, API/export access to assessed platforms already exists, findings stay self-contained (no Service Delivery/PM handoff wiring) for now.
- Retired Python (pandas) report scripts entirely on conversion to PowerShell rather than keeping them alongside (matches precedent from the project-management conversion) - `ticket_summary_flags.py` was deleted 2026-07-21 after `Export-CoordinatorTicketFlagsReport.ps1` was validated against it field-by-field. Same "retire, don't keep alongside" call applied again 2026-07-28 to the old Huntress script.
- Huntress API quirk: its `status` query param rejects values like `sent` as invalid when tested live - status filtering has to happen client-side against the full fetched list (`.Where({ $_.status -ne "resolved" })`), not server-side. Also: company/org ID `0` can be a real account in Autotask (ServIT's own internal company) - `Where-Object { $_ }`-style truthy filters silently drop it; use `$null -ne $_` instead when building lookup batches from IDs that could legitimately be zero.
- Validation bar for any port/rewrite in this workspace: field-by-field diff against a reference/original run, not spot checks - caught two real bugs in the ticket coordinator port (a double-counting bug and a rounding-vs-truncation mismatch between Python and PowerShell) that casual review would have missed. Same rigor applied 2026-07-29 to the dispatcher hardening (every new guard tested live in both directions before considered done, including a fresh fake multi-script manifest entry to actually observe the inter-script delay firing).
- PowerShell gotcha worth remembering for any future manifest `Arguments` use: array-splat (`@array`) binds positionally only - use a hashtable splat (`@hash`) for named/switch parameters, or they silently misbind.
- `Export-Utf8NoBomCsv` usage settled, 2026-08-17: its only proven justification is byte-for-byte parity with the retired Python coordinator reports (now moot), and it provides zero benefit on `ait-m365`'s purely-internal `data/raw/*.csv` files (`Import-Csv` handles BOM either way) - flagged as possibly worth reverting those to plain `Export-Csv`. Brad's call: leave it everywhere it's currently used rather than touch it further - "better to have it and not need it than the reverse," especially now that the empty-collection bug is actually fixed. Don't re-raise this as an open question; it's decided.
- **Orchestrator/wrapper switch-plumbing scope decided, 2026-08-18**: don't thread every Analyst switch (`-IncludeGuests`, `-LessThanDays`, `-Testing`, etc.) up through the orchestrator into the customer wrapper preemptively - speculative with only one real customer (Katz) today, and it's plumbing that has to be kept in sync by hand every time an Analyst gains a new switch. When a second customer actually needs a different value, add that one param to that one wrapper+orchestrator pair - or if the need recurs across customers, prefer a data-driven per-customer override (matching `CustomerMap.csv`/exclusion-CSV precedent) over more parameter-threading. Applied immediately to the two already-inconsistent cases: `Compare-GroupMembers-Admins.ps1`'s `[switch]$IncludeDisabledUsers` now defaults `$true` itself (was forced via a hardcoded `-IncludeDisabledUsers` in `Invoke-GroupAudit-Admins-Default.ps1`, now removed) - single source of truth, matching how `Compare-MailboxSize.ps1` already owns its own `$MinStoragePercentage = 75` default rather than the orchestrator duplicating it. Re-verified: same 9-membership result as every prior run, confirming the default truly didn't change, just where it lives.

## What's next

Brad is continuing to get the coordinator reports created, filtered correctly, and actionable across all three teams. Production's central dispatcher task now confirmed working end-to-end with all three teams' coordinators (PM, service-delivery, security) in the manifest and actually firing on their schedules - the direction is consolidating onto the one dispatcher task rather than a separate Windows Task per team. `program-management/ait-m365` is now at 17 audits (grew from 8 across several work sessions on 2026-08-17/18, some on a different machine), 15 in the Collector→Analyst→Default-orchestrator→customer-wrapper shape and 2 (the log exports) Collector-only by design, all run at least once against real Katz data. What's still open there: only temp-groups, admin-group, and CA-group-members have had their actual audit *logic* diff-checked against original retired scripts (not just "does it run without erroring") - the other 14 haven't had that deeper review. Also still open: verify actual email delivery from a real network, decide when to add any of the 17 to `ScriptManifest.csv`, bring more customers onto the same shape now that onboarding is just a `CustomerMap.csv` row plus one small wrapper file per audit, and (Brad's flagged idea) an eventual Analyst layer over the two log exports for CA-bypass/bad-actor detection or failed-vs-successful sign-in trends. Elsewhere: decide whether `ait-patching` becomes a recurring monthly run or stays a true one-off.
