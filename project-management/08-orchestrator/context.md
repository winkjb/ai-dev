# project-management/08-orchestrator

## Purpose
Cross-cutting coordinator — final section in `../pm-agent-architecture.md`. Routes tasks between agents; resolves conflicting outputs; decides what surfaces to a human vs. resolves autonomously.

## Contents
(empty — to be populated)

## Notes
Per the architecture doc's build sequence, wire this in incrementally as real conflicts start appearing between agents, rather than pre-building every resolution rule speculatively.
