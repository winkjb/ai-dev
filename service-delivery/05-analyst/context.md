# service-delivery/05-analyst

## Purpose
Proactive Tier — Agent #5 in `../support-service-delivery-agent-architecture.md`. Detects cross-ticket patterns (e.g. N tickets this month referencing the same firewall model's memory conserve mode); flags emerging issues; distinguishes one-off incidents from systemic problems.

## Contents
(empty — to be populated)

## Notes
Flagged as **build-sequence priority #3**, given direct relevance to the current firewall fleet issue. Architecture doc suggests this could be the *same* Analyst agent as `project-management/05-analyst`, querying both project and ticket data for a unified cross-org view — worth deciding before building a second, separate implementation.
