# service-delivery/07-resource-allocation

## Purpose
Proactive Tier — Agent #7 in `../support-service-delivery-agent-architecture.md`. Matches tech assignment to skillset across both ticket and project work; surfaces skillset gaps (e.g. certifications like FortiManager, Huntress) for hiring/training decisions.

## Contents
(empty — to be populated)

## Notes
Flagged as a **strong candidate to share** with `project-management/07-resource-allocation` — it's the same tech pool either way, since techs split time between reactive ticket work and project work. The architecture doc calls this out as worth the extra up-front design effort to unify rather than build twice.
